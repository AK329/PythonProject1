def analyze_patient_condition(hr, bp, gluc):
    # এআই ট্রায়াজ লজিক (সৃজনশীল সমাধান)
    if (hr > 120 or hr < 50) and (bp > 150):
        return "CRITICAL 🚨", 2 # রেড এলার্ট
    elif hr > 100 or bp > 140 or gluc > 180:
        return "Warning ⚠️", 1 # ইয়েলো এলার্ট
    return "Stable ✅", 0 # গ্রিন সিগন্যাল